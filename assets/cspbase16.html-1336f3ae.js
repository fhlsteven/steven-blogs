import{_ as a,o as s,c as n,a as e}from"./app-8e5157a8.js";const p={},o=e(`<h1 id="从数组中移除元素" tabindex="-1"><a class="header-anchor" href="#从数组中移除元素" aria-hidden="true">#</a> 从数组中移除元素</h1><p>实现：</p><p>当试图从数组中间移除元素，删除看上去是一个很好的解决办法。但是，当把这个方法使用在数组元素的时候，简单的删除只能删掉元素的内容，但是数组的空间仍旧保留，没有删除。</p><p>如果需要移除的元素位于数组的开头或者结尾部分，你可以分别使用<code>shift</code>和<code>pop</code>来移除它们。使用功能函数<code>splice</code>可以移除数组中处于中间部分的元素，在删除元素的同时删除其所占用的空间。</p><p>使用<code>splice</code>函数的时候，传递所要修改的数组、开始的地方、删除的个数。例如，下面这个程序用于删除所有小于等于3个字符的元素：</p><div class="language-perl" data-ext="perl"><pre class="language-perl"><code><span class="token variable">@array</span> <span class="token operator">=</span> <span class="token string">qw(now is the time for all good men to come to the aid of their country)</span><span class="token punctuation">;</span>
<span class="token keyword">print</span> <span class="token string">&quot;Before: @array\\n&quot;</span><span class="token punctuation">;</span>
<span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token variable">$i</span> <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span> <span class="token variable">$i</span> <span class="token operator">&lt;</span> <span class="token variable">@array</span><span class="token punctuation">;</span> <span class="token variable">$i</span><span class="token operator">++</span><span class="token punctuation">)</span>
<span class="token punctuation">{</span>
    splice<span class="token punctuation">(</span><span class="token variable">@array</span><span class="token punctuation">,</span> <span class="token variable">$i</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">)</span> <span class="token operator">and</span> <span class="token variable">$i</span><span class="token operator">--</span> <span class="token keyword">if</span> <span class="token punctuation">(</span>length <span class="token variable">$array</span><span class="token punctuation">[</span><span class="token variable">$i</span><span class="token punctuation">]</span> <span class="token operator">&lt;=</span> <span class="token number">3</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token keyword">print</span> <span class="token string">&quot;After: @array\\n&quot;</span><span class="token punctuation">;</span> 
</code></pre></div><p>程序的输出结果如下：</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>Before: now is the <span class="token function">time</span> <span class="token keyword">for</span> all good men to come to the aid of their country
After: <span class="token function">time</span> good come their country
</code></pre></div><p>当功能函数<code>splice</code>移除元素的时候，它把其它的数组中的元素前移（注意：我们在每次运行<code>for</code>语句删除元素的时候，都需要备份，这样我们就不会遗漏任何的元素）</p><p>功能函数<code>splice</code>是一个删除数组中间元素的简单方法。</p>`,10),t=[o];function c(l,r){return s(),n("div",null,t)}const u=a(p,[["render",c],["__file","cspbase16.html.vue"]]);export{u as default};
